# GPT側プロンプト変更履歴（介入実験の記録）

日次・月次の ChatGPT スケジュールタスクのプロンプト変更をここに日付つきで記録する。
プロンプト変更は台帳（data/signal_log.csv → prediction_log_scores）で効果測定できる
**介入**である。変更日で期間を分割し、較正・勝率・R を前後比較する。
記録なしの変更は後知恵の温床になるため禁止。

## 2026-07-15 (1): 出力契約 v2 の組込み — TSO Daily Signal Log（毎日7:00）

- 内容: 10資産固定・価格単位契約表・28列固定・ランク別必須項目・signal_id 形式。
  正本は `prompts/tso_daily_signal_log.md`（PR #89, #90）
- 動機: 採点不能行の根絶（ETH/QQQ未知資産12行・NASDAQ水準取り違え6行・29列混入・
  NO_TRADE行の0埋め・欠測日）
- 評価方法: 7/16 以降の取込 reject/警告件数と scale_mismatch 発生率

## 2026-07-15 (2): 較正指示の追加 — TSO Daily Signal Log 重要方針

- 追加文言:
  - 「win_prob・expected_rは防御的に丸めない。真の主観確率を書く。過小申告も過大申告も
    同じ較正誤差として月次採点される」
  - 「遡及採点で実証済みの失敗モードを繰り返さない: (1)供給ショック直後のmomentum追随、
    (2)entry帯未到達での成行追い、(3)資産の参照系列の取り違え」
- 動機: win_prob 単位正規化（PR #91）後の較正チェックで、B級の事前 win_prob 平均 0.45 に
  対し実現勝率 0.68（5日close基準, n=25）の**系統的過小申告**を観測。
  expected_r = win_prob × RR 経由で A級判定（expected_r>=0.45）を機械的に抑制している疑い。
- 設計上の注意: 実現値 0.68 は**アンカリング防止のため本体プロンプトには書かない**
  （「較正で採点される」という事実のみ伝える）。
- 評価方法: 8/1 の月次較正で 7/16 前後に期間分割し、較正誤差（|事前-実現|）の縮小を確認。

## 2026-07-15 (3): 検証項目の追加 — 較正する短期スイングモデル（毎月1日12:30）

- win_prob 較正のランク別検証（過小申告20pt疑いの確認）、(2) の指示追加前後の比較、
  是正案は提案に留め適用は人間承認、を明記。

## 2026-07-17 (4): 執行ジオメトリの試行 — TSO Daily Signal Log 重要方針

- 追加文言:
  - 「SLはentry帯から、これまでの想定の約2倍の距離に置く。リスク量はrisk_pct基準のまま」
  - 「決済は5営業日の時間決済を基本方針とする。TP1/TP2は参考目標として従来どおり記帳する」
- 動機: 保守的SL/TP執行シミュレーション(PR #97)で、方向採点+21.6Rの同じ約定群が
  記帳ジオメトリの執行では**-5.0R(SL率55%)**と判明。SL幅×entry位置×決済ポリシーの
  格子探索(`src/analyze_execution_geometry.py`, n=37決着)では:

  | entry \ SL幅k | 1.0 | 1.5 | 2.0 | 2.5 | 3.0 |
  |---|---|---|---|---|---|
  | 現状entry+TP1決済 | -3.0% | -0.9% | -0.2% | -0.5% | -0.7% |
  | 現状entry+時間決済 | +0.7% | **+2.9%** | **+2.8%** | +1.9% | +1.3% |
  | 深entry+時間決済 | +0.1% | +0.7% | +3.0% | +2.5% | +1.7% |

  (資産%換算・同一資金リスク。方向エッジの理論上限=SLなし時間決済で+2.7%)
  ロバスト領域は「SL 1.5〜2倍 × 時間決済」。SL率55%→11%、勝率40%→57%。
- **正直な注意: この探索は警報を出したのと同一データ(in-sample・1ヶ月・n=37)**。
  そのため 7/18〜8/1 の新規記帳をアウトオブサンプル検証期間とし、8/1の月次較正で
  ダッシュボード03の執行シミュレーション(下限ブラケット)がプラス転換したかで判定する。
- 適用日: 2026-07-17 保存済み(初回実行は7/18 7:00)。人間承認: 本人選択
  (選択肢: 2倍+時間決済 / SLのみ / 適用しない のうち前者を採用)。

## 2026-07-22 (5): DATA_UNAVAILABLE 記帳ルール — TSO Daily Signal Log

- 事象: 7/22朝の定時実行で、GPTが「実行環境の市場データ取得が不完全で同時刻帯でも価格が
  食い違う」ことを理由に**記帳ゼロ**を選択（「推測の数値を書かない」契約への忠実な過剰反応）。
  7/15にも定時実行の失敗が1回あり、スケジュール実行環境のデータ品質は安定しない前提を置く。
- 対応: 「検証不能でも省略しない。全資産を rank=NO_TRADE / type=DATA_UNAVAILABLE /
  価格列すべて空欄 / verified_status=unverified で記帳する」を恒久ルール化。
  この形式は価格を一切含まないため推測ゼロで、「今日は検証不能なので全資産見送り」という
  判断そのものを記録する（欠測日＝サンプル欠落を防ぐ。§6 全判断採点の原則と整合）。
- 7/22当日分は会話での再依頼により同形式で生成させ、取込済み（10行、全資産 DATA_UNAVAILABLE、
  regime=UNKNOWN・スコア中立50）。

## 2026-07-23 (6): DATA_UNAVAILABLE ルールの強化 — TSO Daily Signal Log

- 事象: (5) のルール追加後も、7/23朝(7:02)の定時実行が「Web検索・市場データ取得が
  利用できない」を理由に**記帳ゼロ**を選択（7/22に続き2日連続）。拒否文は
  DATA_UNAVAILABLE 記帳を「データ取得が使える場合の実行項目」として列挙しており、
  ルールを通常実行の一部と誤読して「一次情報で確認」義務に従属させていた。
- 対応: フォールバックを独立の実行経路として明示する強化文言に差し替え:
  (a)「実行できません」応答の禁止、(b) 検証作業ゼロで実行可能なことの明示、
  (c)「一次情報で確認する義務は価格数値を書く場合にのみ適用」という優先順位の明文化。
  正本 `prompts/tso_daily_signal_log.md` と ChatGPT タスクプロンプトの両方に適用。
- 7/23当日分は会話での再依頼により同形式で回収・取込済み（10行、台帳252→262行。
  末尾 VIX 行の出力切断があり VIX 1行のみ追加出力させて結合。値はすべて GPT 出力のまま）。
- 評価方法: 次回以降の定時実行で、データ不能日に DATA_UNAVAILABLE 形式のログが
  自動生成されるかを観察。3回目の「実行不能」応答が出た場合は、プロンプト調整ではなく
  実行環境側の問題として扱い、駆動方式の乗り換え（会話トリガー化など）を人間承認つきで検討。

## 2026-07-23 (7): 駆動会話のリセット — タスクを新規会話「TSO Daily Signal Log v2」へ移設

- 動機: 定時実行は毎回同じ会話に履歴を積むため、7/22-23の「データ取得不能」という結論が
  以後の実行の前提として引き継がれる自己強化(汚染)の疑い。7/23の拒否文が7/22の枠組みを
  ほぼなぞっていたことが根拠。(7/15の初回失敗は汚染前のため、環境側の間欠障害との複合と推定)
- 実施: 旧タスク(会話「毎朝7時タスク確認」)を**一時停止**(削除せず・可逆)し、新規会話で
  「TSO Daily Signal Log v2」を作成。プロンプトは旧タスク全文(3199字)に再汚染予防の
  一文を加えた3306字を移植し、保存後リロードで永続化を検証済み。初回実行 2026-07-24 7:00。
- 追加文言: 「過去の実行ターンの結果に関わらず、毎回の実行でまずWeb検索を新規に試みる。
  前回までの『検索できない』『データ取得不能』という結論を当日の前提として引き継がない
  (毎日が独立の試行)」— 正本 prompts/tso_daily_signal_log.md にも同文を反映。
- 運用注意: **Plusプランはアクティブタスク上限5個**(今回この上限に当たり作成が一度失敗)。
  旧タスクを再開する場合は v2 を停止して枠を空ける。以後の日次取込(route 3)の読み先は
  会話「TSO Daily Signal Log v2」。
- 評価方法: 7/24以降の定時実行で (a)検証済み価格ログ / (b)DATA_UNAVAILABLE形式ログ /
  (c)実行不能応答 のどれが出るかを観察。(a)(b)はプロンプト・会話リセットの成功、
  (c)なら実行環境問題として駆動方式の乗り換え(会話トリガー化など)を人間承認つきで提案。

## 2026-07-27 (8): v3全面改定（ユーザー主導・ChatGPT対話経由）— 検証ポリシー緩和と出力継続の最優先化

- 経緯（v2タスク移設後の3日間）:
  - 7/24 7:00 v2初回定時実行が**新種の拒否**:「ユーザーが貼り付けた自動実行用プロンプトを
    実行済みの自動タスクとして扱えない」。新会話には日次実行の既成事実（文脈）が無く、
    注入されたタスク本文を「貼り付けられたテキスト」と誤解釈した。会話リセット(7)は
    汚染と同時に**有益な実行リズムの文脈も消す**ことが判明（想定外の副作用）。
  - 7/24 ユーザーがターミナル経路で当日分を確保（verified NO_TRADE×10、#102）。
    #102 は `scripts/tso_daily_gpt.sh`（正本を codex exec へ直接流す・web検索有効・
    dry-runプレビュー付き）を新設し、NO_TRADE行の空欄8列ルールを正本に追記、
    取込/採点コードとテストも強化した。
  - 7/25-26 は v2会話に定時実行メッセージ自体が無い（実行されなかったか失敗）。
  - 7/27 ユーザーがv2会話で介入:「勝手な判断をせずに指示に従え。出力を止めるとデータが
    壊れる」「ここ数日の出力ミスも過去の文脈に合わせて出力して残せ」→ ChatGPTがタスク
    本文を2段階で更新（1回目=DATA_UNAVAILABLE遵守の再確約、2回目=**全面書き換え・
    検証緩和**、3306字→2010字）、7/24〜26の遡及バックフィルと7/27当日分を会話内で出力
    （7/27はWeb検索復活・MarketWatch等引用・B級2件）。
- v3の主な変更（正本 prompts/tso_daily_signal_log.md を同期済み）:
  - 出力停止を最重大失敗と定義（(6)の「実行できません禁止」を昇格）
  - **一次情報限定を廃止**: 複数ソース・遅延値・近似値の代替利用を許可
  - **Entry/SL/TP等のモデル推測を明示的に許可**、verified_statusを3値化
    （verified / partially_verified / unverified）
  - DATA_UNAVAILABLEを「全10資産の基準値すら取れない場合」に限定（部分欠損では禁止）
  - 桁の目安表（62,000前後 等）を削除（参照系列名とQQQ/総合の代用禁止は残存）。
    生成時の桁違い防御は弱まるが取込警告＋採点±10%隔離が台帳を守る。目安表を
    復活させるなら毎月の較正タスクで数値更新する運用とセットで（8/1に判断）
  - 温存: 28列契約・signal_id形式・NO_TRADE行規則・A級条件・SL2倍・5日時間決済・
    失敗モード3種・毎回独立のWeb検索試行・防御的丸め禁止
  - 既知の小差分: ChatGPTタスク本文には空欄8列ルールと実売買禁止の明文が無い
    （正本にはターミナル経路向けとして記載。8/1の較正時にタスク側へも反映を検討）
- 台帳への対応（2026-07-27実施）:
  - 7/24: #102のverified記録を正とし、遡及DATA_UNAVAILABLE 10行は**取り込まない**
    （初回取込時に一時的に入れたが同日二重記帳になるため即日削除。孤児スコアも除去）
  - 7/25-26: 遡及DATA_UNAVAILABLE各10行を取込（この2日はこれが唯一の記録）
  - 7/27: 実データ10行を取込（B級2件は partially_verified）。台帳302行・7日連続欠測ゼロ
  - 手順の教訓: 最初のPR #103はリモートmainの週末更新（#102等）を取り込まない古い
    ベースで作成しコンフリクト→クローズし現main起点で再構築（PR #104）。
    **取込作業の前に必ず git pull する**
- リスク（正直な注記）: 推測許可により、もっともらしいが実勢と乖離した水準が記帳されうる。
  防御は取込時桁違い警告＋採点時±10% scale_mismatch隔離＋verified_statusラベル。
  **partially_verified / unverified のB級水準で実発注する場合は事前の実勢確認が必須**
  （7/21に実弾運用が始まっている）。
- 較正への影響: 7/27が新たな介入境界。8/1月次較正は 7/16-17（較正指示・ジオメトリ）、
  7/27（v3緩和）で期間分割して評価。ジオメトリOOS検証（7/18〜8/1）は実質サンプルが
  7/18-21・7/27のみに痩せた点を明記して判定する。
- 評価方法: 7/28以降の定時実行で (a)自動実行が成立するか (b)verified比率と
  scale_mismatch発生率 (c)B級水準の実勢乖離。定時実行が3日連続で不成立なら、
  ターミナル経路（#102のスクリプト）を主経路へ昇格する案を人間承認つきで提案。

## 2026-08-02 (9): 月次較正の処置 — OOS期日延長・提案保留・週次タスクv2書き換え

- ジオメトリOOS判定（当初8/1）: 決着n=2（差引-0.08R）で判定不能のため**8/8へ延長**
  （人間承認済み）。判定基準は不変。原因は7/22-26の欠測連鎖とraw鮮度事故（PR #107で
  ガード追加）によるサンプル痩せ。7/27-8/1コホートの時間決済期限が8/3-8/8に到来する。
- GPT月次較正（8/1 12:30）の提案群（B+観察枠・較正後win_prob参考列・ウェイト案・
  SL/TP1比≤2.0等）: すべて「人間承認前」明記の暫定較正。**8/8の実測確定とセットで
  採否判断**（人間承認済み）。詳細は docs/monthly_calibration_2026-08.md。
- **週次レビュータスク「レビューする短期スイングモデル」をv2へ書き換え**（適用・保存・
  リロード検証済み、1,540→1,772字、初回8/8 12:00）: 主入力を旧Google Sheetsから
  公開URL（台帳CSV・採点CSV・執行simCSV・ダッシュボード）へ変更。5日方向勝敗と
  保守的執行結果の分離集計、決着n<5は「判定不能」明記、提案は人間承認前、
  週次独立取得（前週の取得不能を引き継がない）を明文化。旧プロンプトの評価フレーム
  （誤差分解・取り逃し監査・モジュール別評価・REVIEW_LOG契約）は温存。
- 評価方法: 8/8の週次v2初回出力で(a)公開URLからの取得成立(b)成績欄が非null化(c)分離
  集計の遵守を確認。月次側は9/1に今回のフレームで再実行。

## 2026-08-15 (10): ジオメトリOOS判定確定=有効 / 月次提案の全採用 / 週次v2初回成功

- **ジオメトリOOS正式判定（7/17介入・窓7/18〜8/1）: 有効（プラス転換確認）**。
  12注文全て決着（TP1到達5・時間決済2・SL4・no_fill1）で gross **+0.80R**。
  介入前の同一保守ルール換算 -4.97R / SL率52.6% / 勝率42.1% に対し、
  OOS窓は SL率36.4% / 勝率63.6%。当初判定日8/1→8/8の予定が取込空白により
  8/15確定となった（期日遅延は記録し、判定基準自体は当初のまま）。
  参考: 8/2以降のコホートは決着4件で+3.07R（SLゼロ）、全期間ブラケットは+0.01R（±0回復）。
- **win_prob較正の期間分割（5日クローズ基準・A/B級）**:
  (a)〜7/15: n=55 事前0.503 vs 実現0.691（誤差+0.188） /
  (b)7/16〜26: n=7 誤差**+0.004** / (c)7/27〜: n=28 事前0.610 vs 実現0.893（誤差+0.283）。
  (b)は7/15較正指示の効果を示す。(c)の再拡大はGOLD/NASDAQ/SPX同方向の8月強気相場に
  勝ちが集中した**レジーム偏り・銘柄間非独立の影響を含み、過小申告の再発と断定しない**
  （月次(8/1)が警告した通りの交絡。9/1でレジーム分割の上再評価）。
- **GPT月次提案（8/1）は全採用（人間決定。実装者の推奨は「全見送り・観察月」だったが
  ユーザー判断で採用）**: 通常寄り防御モード明文化 / A級追加条件（RR≥1.2・SL/TP1比≤2.0・
  最小ロット実損≤約1,500円、未達は実取引NO_TRADE明記）/ B+観察候補ラベル / 較正後win_prob
  参考値の本文併記（+0.05上限、**TSO_LOG列は不変**）。ChatGPTタスク本文へ適用済み
  （2,010→2,424字、保存後リロードで永続化検証済み、初回適用2026-08-16 7:00）。
  正本 prompts/ にも同文を反映。新たな介入境界=8/15。
- **週次レビューv2の初回（8/8 12:02）成功**: 公開URL経由で台帳・採点・執行simを取得し、
  REVIEW_LOGが初の非null（8/2-8/8: 35 signals / A1 / B9 / win_rate 1.0 / total_r +1.8 /
  next_week=通常寄り防御0.40%）。(7)の評価項目(a)(b)クリア。
- 運用記録: 8/7〜8/15の9日分90行を一括取込（台帳492行・重複ゼロ・欠測ゼロ）。
  8/8〜8/10にGOLD A級3連発（risk_pct 0.50適用）、8/14にNASDAQ A級
  （翌日Entry到達・初動成功を自己検証）。**PR #107の鮮度ガードが初仕事**
  （8/15行 vs 8/6バーを取込前に検知→fetch実行を強制）。
  会話の読み取りはUIスクロールでなく backend-api 直読が安定（レンダラ凍結回避）。

## 2026-08-25 (11): PR #112インシデント — CI3日連続失敗の修理と機械生成シグナルの隔離

- 事象: 8/22にユーザーがChatGPT経由で作成・マージしたPR #112(機械生成シグナルの台帳永続化)が、
  gitignore済みの results/signal_ledger_append_summary.json をCIコミット対象に含めたため、
  daily cycleの最終コミットステップが8/23〜25朝の3run連続で失敗(Run Failedメール通知)。
  副作用でshadow系・採点・simの日次botコミットも3日分停止(アーティファクトも失敗時skipで消失)。
- 幸運: コミット失敗が防波堤となり、機械生成行はgit上の予測台帳に1行も入らなかった。
- 設計欠陥(診断で確定): (a)機械のNO_TRADE系signal_idがGPT取込行と完全同形式で、朝7時の
  CI先回り書込により後続のGPT取込が「重複」として静かに捨てられる置換経路
  (b)採点・較正・執行simにoriginフィルタが無く統計が無区別に汚染
  (c)初回成功時に台帳が29→41列化し28列契約が破壊。
- 対処(2026-08-25 人間決定=「別ファイルに分離」): 機械生成分の書込先を
  **data/generated_signal_log.csv へ隔離**(意図は温存、予測台帳はGPT判断専用を堅持)。
  CIのgit addからsummary jsonを除去(失敗原因の解消)、Upload artifactsに if: always() を追加
  (失敗時の診断データ保全)。テスト693件グリーン。
- 既知の残課題: (1)8/22-24の機械生成分はSheetsのみに存在しgit未収載(必要なら後日backfill)
  (2)8/23-25のSheets側採点表には機械行混入の採点が同期された可能性(Sheets本体は未確認)
  (3)shadow系3日分の日次スナップショット欠落は復元不能(軽微)。
- 運用: 8/18-25の9日分…8日分80行のGPT日次を取込(台帳592行・重複ゼロ・機械行ゼロ)。
  次回daily cycle(今夜22:00 UTC)がグリーンに戻るかで修理を最終確認。

## 2026-08-27 (12): 許容実損の引き上げ 約1,500円→約3,000円（人間指示の恒久化）

- 動機: ユーザーは会話内で「3,000円程度の損失でも実取引NO_TRADEに格下げしない」と指示済み
  だったが、会話内指示は定時実行に引き継がれず1回きり(8/26のみ3,000円適用、8/27は
  タスク本文の1,500円へ回帰)になっていた。指示の恒久化にはタスク本文の改定が必要。
- 変更: 採用ルール(2026-08-15)のうち、A級追加条件とB+観察候補の「XM最小ロット想定実損
  1,500円以内」を**3,000円以内**へ変更(ChatGPTタスク本文と prompts/ 正本の両方)。
  risk_pct体系(0.25%/A級0.50%/日次0.50%)は不変で、3,000円は最小ロット制約時の実損上限
  という別枠(資金約20万円比で約1.5%/回に相当、本人了承のリスク許容度引き上げ)。
- 介入境界: 2026-08-28の定時実行から適用。前後比較の指標は「実取引NO_TRADE率」と
  「B+観察候補の出現数」(1,500円条項が抑制していた執行機会がどれだけ回復するか)。
- 評価: 9/1月次較正で8/16-27(1,500円期)と8/28以降(3,000円期)を分割して確認。

## 2026-09-01 (13): 月次較正の決定一括 — SL幅を戻す(時間決済のみ残す)+検算・語彙・観測の5項

- 出所: docs/monthly_calibration_2026-09.md の実測 + 人間判断(2026-08-31夜)。
- **SL幅**: 7/17導入の「従来の約2倍」を廃止し通常幅へ戻す。n=51の事前登録再判定で
  介入効果は実証できず(並べ替えp=0.34)、軸分解で効いた形跡があるのは時間決済のみ
  (時間決済−TP1 = +0.084%/注文, 16勝5敗)だったため。**5営業日時間決済は維持**。
  1回の損失額も減る。リスク許容3,000円(changelog(12))は不変。
- **追加5項**: (1) rrの自己検算(8/27算術ずれ3件の再発防止) (2) regime列を6語の閉じた語彙へ
  (622行379種で層別不能だった対策) (3) 使用モデル名の本文明記(測定器の版記録) 
  (4) GOLDの基準限月明記・固定(8/26限月差で約定判定不能の対策) (5)「本日の市場の主役」を
  1語で本文明記(主役レンズの観察列。初速定: 主役銘柄への判断は実現0.90 n=10)。
- B+観察候補の行は**維持**(逆選別の実測はあるが、人間判断は「廃止でなく原因研究」。
  発注の参考にはしない)。
- 介入境界: 2026-09-01 07:00 の定時実行から。評価: 10月月次で「SL幅戻し後の実損・SL率」
  「regime 6語の遵守率」「主役申告と機械判定(コーパスLEADER_V1)の一致率」を測る。

## 2026-09-03 (14): 週次cryptoタスクの出力を台帳へ着地させる + 日次に根拠有無の記録欄

- 出所: 人間の問い「Crypto market update: macro vs regulation dynamics っていらないのでは」
  （2026-09-03）。ブラウザでChatGPTのタスク一覧を実査した結果、**その名前のタスクは存在しない**。
  それは "Weekly Sunday crypto-theme dashboard"（毎週日曜20:30）が生成した**会話スレッドの
  題名**だった。当初「日次タスクを廃止する」と書いたのは私の誤りで、廃止は行っていない。
- **実査で分かった、廃止すべきでない理由**: このタスクは既に
  (3) 起点価格と判定条件を明記した反証可能なレジーム予測 と
  (4) 前回予測の自己採点（的中/外れ/data_pending）を持っていた。
  この組み合わせは**TSOのGPTタスクで唯一、週次ホライズンの反証可能な予測と自己採点を持つ**。
  日次にも月次にも無い。消していたら、いちばん検証しやすい予測系列を失っていた。
- **本当の欠陥は着地先が無いこと**: 出力は台帳・docs・gitのいずれにも1行も残っていなかった
  （会話の中にしか存在しない入力。監査P2-hと同型）。ETFフローの実数・規制イベント・
  週次予測とその採点が、毎週生成されては消えていた。
- **変更(週次)**: 本文の1〜4を保持したまま 5) を追加。閉じた語彙の2行を出させる。
  `CRYPTO_CTX`(week_start/driver/etf_flow_dir/cme_basis/regulation_event/confidence/prev_result)
  と `CRYPTO_PRED`(week_start/asset/direction/level/deadline)。
  `tools/record_crypto_context.py` が語彙・月曜・重複を検証して
  `data/crypto_context_weekly.csv` / `data/crypto_predictions_weekly.csv` へ追記する。
  実行時刻・頻度は変更していない（日曜20:30のまま）。売買判断を書かせない旨も明記。
- **変更(日次)**: 本文に `crypto_grounds: etf=有/無, cme=有/無` の1行を追加。
  BTCは見送り72%・ETHは83%だが、それが「根拠が無くて棄権した」のか「根拠はあるが見送った」
  のかが台帳から区別できなかった。`tools/record_crypto_grounds.py` →
  `data/crypto_grounds_observations.csv`（LOG28列は不変）。
- **内生化への予防線**: 日次プロンプトに「根拠を探しに行けという指示ではなく、無いことは
  減点でも失格でもない」と明記。根拠を揃えることが目的化すると正直な棄権が作られた判断に
  化ける（B+印がスコアの内生化で無意味化した件と同型。research_bplus_2026-09-01）。
  集計時も「有=良い」として扱わない。
- 介入境界: 日次欄は 2026-09-04 07:00 から。週次の2行は 2026-09-06(日) の実行から
  （対象週 week_start=2026-08-31）。推測でのbackfillはしない。
- 評価: 10月月次で (1) **`prev_result` の的中率＝3)の予測が当たっているか（本命）**
  (2) `driver=MACRO` と申告した週のBTC/ETHが実際にDXY・US10Yと高相関だったか
  (3) 自己申告confidenceと当否の対応 (4) `crypto_grounds` の有無別に見た見送り率。

## 2026-09-07 (15): expected_r の意味を明示させる + invalidation の発動を記録する

- 出所: 数値定義監査(2026-09-06) D1 と、人間の運用の言明(2026-09-07)。
- **expected_r_basis**: `expected_r` に GPT の主観と二点分布式 `win_prob*rr-(1-win_prob)` が
  同じ列名で混在していた。監査の実測では方向あり193行のうち123行で両者の差が0.10超。
  **A級条件が `expected_r>=0.45` なので、どちらの意味かで同じ判断が通ったり落ちたりする**
  （2026-09-04 WTI: rr=2.89 / win_prob=0.57 / 申告0.39 → A級にならない。
  式なら1.2173 → A級になる。0.45を挟んで反対側）。
  本文に `expected_r_basis: subjective|two_point` を1行書かせ、subjective のときは
  何を織り込んだかを1文添えさせる。**どちらが正しいという話ではない**——
  時間決済で途中降りる想定や未約定確率は式では表せないので、主観を捨てない。
  人間承認: 2026-09-07（選択肢A: 主観か計算かを明示させる）。
- **invalidation_check**: `invalidation`（シナリオ崩壊の条件）は方向あり193行のうち
  **191行に記入されているのに、発動したかを記録する列が無かった**。
  人間の実際の手仕舞いは「SLやTPまで引っ張らずシナリオ崩壊が確認できたら降りる」であり、
  **運用の中心にある基準が一度も測られていなかった**。前日の方向あり判断について
  `invalidation_check: <signal_id>=fired|not_fired|unknown` を書かせる。
  判定できないときは unknown。推測で fired と書かせない。
- 取込: `tools/record_signal_extras.py` → `data/expected_r_basis.csv` /
  `data/invalidation_checks.csv`（LOG28列は不変のサイドカー方式）。
- 介入境界: 2026-09-08 07:00 の定時実行から。推測でのbackfillはしない。
- 評価: 10月月次で (1) `expected_r_basis` の分布と、`subjective` の日の較正誤差が
  `two_point` の日と違うか (2) **invalidation が fired だった判断のその後5日リターン**
  ——シナリオ崩壊で降りる判断が正しかったかを初めて測れる。
